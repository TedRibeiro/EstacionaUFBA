<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matc89.estacionaufba">

    <!--
     The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
     Google Maps Android API v2, but you must specify either coarse or fine
     location permissions for the 'MyLocation' functionality.
-->
    <uses-permission android:nome="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:nome="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:nome="android.permission.INTERNET" />

    <application
        android:nome=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:nome=".activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:nome="android.intent.action.MAIN" />

                <category android:nome="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:nome=".activity.LandingActivity"
            android:label="Koope"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:nome=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:nome="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <service
            android:nome=".meta.HandleLocationIntentService"
            android:exported="false"></service>
        <service
            android:nome=".meta.HandleRouteIntentService"
            android:exported="false"></service>
    </application>

</manifest>